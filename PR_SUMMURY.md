# PR Summary

## 大きな実装・修正方針
- 敵管理ロジックをクラス継承構造にリファクタリングしました。
- 敵個体ごとに `BasicEnemy` と `JumpingEnemy` の2種類を実装し、`ENEMYMANAGE` から動的に生成・破棄するよう変更しました。
- 各種マジックナンバーを `EnemyConstants.h` にまとめ、ゲーム全体で共有できるようにしました。
- コメントを日本語化し、すべてのソースコードを Shift-JIS から UTF-8 へ変換しました。

## 具体的な実装内容
- `Enemy.h` に基底クラス `Enemy` と派生クラス `BasicEnemy`、`JumpingEnemy` を追加。
- `Enemy.cpp` では敵生成・更新処理をこれらのクラスを用いて書き換え、ジャンプ処理や更新関数を実装。
- `EnemyConstants.h` を新規追加し、移動速度やスポーン間隔などの定数を定義。
- `Game.cpp` と `Game.h` では主にコメント修正と関数名の日本語化、敵更新処理の呼び出しに合わせた調整を実施。

## 影響範囲
- 変更ファイル: `Enemy.cpp`, `Enemy.h`, `EnemyConstants.h`, `Game.cpp`, `Game.h`
- 主に敵管理周辺のロジックを中心に修正しており、他クラスとのインターフェースは基本的に変わっていません。
- 既存の `cBase` 配列を保持しているため、大きな外部インターフェース変更はありません。

## リファクタリング方針
- 継承を用いた構造に変更することで、敵ごとの挙動追加を容易にしました。
- マジックナンバーの排除と定数化により、可読性とメンテナンス性を向上させました。
- 不要となった処理は `CleanupEnemyInstance` で統一的に破棄するよう整理しました。

